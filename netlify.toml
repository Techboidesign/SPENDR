[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# React Router — client-side routes (e.g. /budget, /settings/profile)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
