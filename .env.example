# Copy to .env.local for development (never commit .env)
# Add the same keys in Netlify: Site settings → Environment variables

VITE_SUPABASE_URL=https://njodyldzvtaberaadwjd.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_or_publishable_key_here

# Site URL for password reset emails (also set in Supabase Auth → URL Configuration)
VITE_SITE_URL=https://spendr-finance.netlify.app

# OCR.space free API key — receipt photo scan & document upload (no OpenAI tokens)
# Register: https://ocr.space/ocrapi/freekey
VITE_OCR_SPACE_API_KEY=
# 1 = fast/clean scans, 2 = receipt photos (default), 3 = hard/handwriting
VITE_OCR_SPACE_ENGINE=2
